<Window x:Class="RhodiumVault.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rhodium Vault"
        Width="720" Height="640"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BgBrush}"
        PreviewMouseDown="Window_Activity" PreviewKeyDown="Window_Activity">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Rhodium Vault" Style="{StaticResource H1}" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Backup" Style="{StaticResource GhostButton}" Margin="0,0,8,0" Click="Backup_Click"/>
                <Button Content="Change password" Style="{StaticResource GhostButton}" Margin="0,0,8,0" Click="ChangePassword_Click"/>
                <Button Content="Lock" Style="{StaticResource GhostButton}" Click="Lock_Click"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox" Style="{StaticResource ModernTextBox}" TextChanged="SearchBox_TextChanged"
                     Tag="Search entries..."/>
            <Button Grid.Column="2" Content="+ Add entry" Style="{StaticResource AccentButton}" Click="AddEntry_Click"/>
        </Grid>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="EntriesPanel"/>
        </ScrollViewer>
    </Grid>
</Window>
